#Instructions for installing and running NodeJS

* run `npm install nodemon -g`
	* if on MacOS and `npm` throws error, run `brew install npm` and then above command.
* run `npm install`
* run `nodemon app.js`
